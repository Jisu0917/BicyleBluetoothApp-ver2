<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@color/black" />	<!-- 테두리 내부 색상 설정 -->

    <!-- 테두리 내부 여백 설정 -->
    <padding
        android:bottom="15dp"
        android:left="3dp"
        android:right="3dp"
        android:top="15dp" />

    <!-- 테두리 설정: 두께, 색상 -->
    <stroke
        android:width="3dp"
        android:color="@color/white" />
    <corners android:radius="50dp"/>
</shape>